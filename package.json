{
  "name": "code-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "start": "npm --prefix backend start",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "docker:build": "docker build -t code-atlas ."
  }
}
